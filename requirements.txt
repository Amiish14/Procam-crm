Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Limiter==3.5.0
SQLAlchemy==2.0.36
Werkzeug>=3.0.6,<4
Jinja2>=3.1.6,<4
gunicorn>=22.0.0,<24
psycopg2-binary==2.9.10
openpyxl==3.1.2
requests>=2.32.4,<3
python-dotenv==1.0.0
# CRM v3.1 additions
anthropic==0.34.2      # AI Outreach (Claude)
rapidfuzz==3.6.1       # Fuzzy header matching for imports
# CRM v3.2 — Microsoft Graph email → lead ingest
msal>=1.28.0           # OAuth 2.0 client-credentials for Graph API
# CRM v2026-09 — Groq / Llama email extractor (OpenAI-compatible API).
# email_ingest/ai_extractor_groq.py imports this; without it the Groq
# extractor silently disables itself and ingestion falls back to regex.
openai>=1.40.0,<2
